export interface SiteMeta {
  name: string;
  summary: string;
}

export const siteMeta: SiteMeta = {
  name: 'PetPlaybook.ai',
  summary: 'PetPlaybook keeps pet care routines, comforts, preferences, and important details ready for trusted caregivers.',
};
